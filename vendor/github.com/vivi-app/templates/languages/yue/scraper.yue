export search =
    title: 'Search'
    subtitle: 'Search results'
    handler: (query, ctx) ->
        {}

export layers =
    * title: 'Layer'
      handler: (media, ctx) ->
        {}

export actions =
    * title: 'Stream'
      description: 'Stream description'
      handler: (medias, ctx) ->

    * title: 'Download'
      description: 'Download description'
      handler: (medias, ctx) ->
