# {{ .Name }} {{ if .NSFW }}🔞{{ end }}

{{ .About }}

> Yue scraper for [vivi](https://github.com/vivi-app/vivi)

## Build

😇 Script

1. Run `./build.sh` in the extension directory.
2. Yeah, that's it.

😈 Manual

1. [Install Yue compiler 📦](https://yuescript.org/doc/#installation)
2. In the extension directory run 🤖

```bash
# Since vivi uses Lua5.1 VM
yue --target=5.1 .
```

3. This will output something like this 📄

```
Built ./tester.yue
Built ./scraper.yue
```

4. It will create compiled `scraper.lua` and `tester.lua` that vivi can use
5. Done!
6. Btw, you can also use `yue --target=5.1 -w .` to watch for changes and recompile on the fly
